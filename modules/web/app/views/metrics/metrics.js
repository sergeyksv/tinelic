define(['tinybone/base','dustc!views/metrics/metrics.dust','highcharts'],function (tb) {
    var view = tb.View;
    var View = view.extend({
        id:"views/metrics/metrics"
    });
    View.id = "views/metrics/metrics";
    return View;
});
